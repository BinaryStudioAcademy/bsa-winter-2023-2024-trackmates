const PromptOutputValue = {
	SORT_COURSES:
		"a sorted list of courses based on their overall quality and interest level, from the most recommended courses to the least. The answer should be without any explanation in the format: [index]. The index starts with 0",
} as const;

export { PromptOutputValue };
