import { CourseValidationRule } from "./course-validation-rule.enum.js";

const CourseValidationMessage = {
	TITLE_MINIMUM_LENGTH: `Minimum title length – ${CourseValidationRule.TITLE_MINIMUM_LENGTH} character`,
};

export { CourseValidationMessage };
