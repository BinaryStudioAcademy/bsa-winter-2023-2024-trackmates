const UserValidationMessage = {
	EMAIL_REQUIRE: "Email is required",
	EMAIL_WRONG: "Email is wrong",
	PASSWORD_SHORT: "Password is too short (8 characters at least)",
} as const;

export { UserValidationMessage };
