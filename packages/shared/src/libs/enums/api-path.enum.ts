const APIPath = {
	AUTH: "/auth",
	COURSES: "/courses",
	RECOMMENDED_COURSES: "/recommended-courses",
	USER_COURSES: "/user-courses",
	USERS: "/users",
	VENDORS: "/vendors",
} as const;

export { APIPath };
