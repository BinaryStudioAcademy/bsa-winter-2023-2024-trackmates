{
	"name": "shared",
	"version": "1.0.0",
	"type": "module",
	"main": "build/index.js",
	"types": "build/index.d.ts",
	"engines": {
		"node": "18.x.x",
		"npm": "9.x.x"
	},
	"scripts": {
		"postinstall": "npm run build",
		"lint:type": "npx tsc --noEmit",
		"lint:js": "npx eslint **/*.ts --no-warn-ignored",
		"lint": "concurrently \"npm:lint:*\"",
		"build": "tsc && npm run build:copy",
		"build:copy": "cp -r package.json build"
	}
}
