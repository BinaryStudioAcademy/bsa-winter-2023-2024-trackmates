import { Config } from "prettier";

const config: Config = {
  printWidth: 80,
  tabWidth: 2,
  useTabs: false,
  semi: true,
  singleQuote: false,
  quoteProps: "preserve",
  bracketSpacing: true,
  arrowParens: "always",
};
